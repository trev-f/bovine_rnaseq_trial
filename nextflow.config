params {
    // directories
    baseDirReports = "${launchDir}/reports"
    baseDirData    = "${launchDir}/data"

    // skip processes
    skipReadsQC   = false
    skipRawFastQC = false
}

singularity {
    enabled = true
}
